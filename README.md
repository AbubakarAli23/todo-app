# Todo App

A small offline todo app built with Kotlin, Jetpack Compose, and Room.

## Features
- Add tasks from the text field
- Mark tasks complete
- Delete tasks
- Tasks persist locally in a Room database

Open the project in Android Studio and run the `app` configuration on an emulator or device (Android 7.0+).
